<%= simple_form_for(@cktest) do |f| %>
  <% if @cktest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cktest.errors.count, "error") %> prohibited this cktest from being saved:</h2>

      <ul>
      <% @cktest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :content, :as => :ckeditor, :input_html => { :ckeditor => { :height => 600, :width => 600 } } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
